"use client";

import { Navbar } from "@/components/Navbar";

/**
 * @deprecated Use Navbar component directly instead.
 * This component is kept for backward compatibility.
 */
export function WizardHeader() {
    return <Navbar />;
}
